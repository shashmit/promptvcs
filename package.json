{
  "name": "promptvcs",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "db:push": "cd apps/api && bun drizzle-kit push",
    "db:studio": "cd apps/api && bun drizzle-kit studio"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "overrides": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0-next.6"
  }
}
